apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: currency-service-prod
  namespace: argocd
  labels:
    app.kubernetes.io/name: currency-service
    app.kubernetes.io/part-of: maliev
    app.kubernetes.io/environment: production
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: maliev-prod
  source:
    repoURL: 'https://github.com/MALIEV-Co-Ltd/maliev-gitops.git'
    targetRevision: v1.0.0
    path: 3-apps/currency-service/overlays/production
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: maliev-production
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
