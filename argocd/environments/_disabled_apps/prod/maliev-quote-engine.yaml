apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: maliev-quote-engine-prod
  namespace: argocd
  labels:
    app.kubernetes.io/name: quote-engine
    app.kubernetes.io/part-of: maliev
    app.kubernetes.io/environment: production
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: "https://github.com/MALIEV-Co-Ltd/maliev-gitops.git"
    targetRevision: v1.0.0
    path: 3-apps/maliev-quote-engine/overlays/production
  destination:
    server: "https://kubernetes.default.svc"
    namespace: maliev-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
