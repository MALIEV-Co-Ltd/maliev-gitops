apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: line-chatbot-service-prod
  namespace: argocd
  labels:
    app.kubernetes.io/name: line-chatbot-service
    app.kubernetes.io/part-of: maliev
    app.kubernetes.io/environment: production
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'git@github.com:MALIEV-Co-Ltd/Maliev.LineChatbotService.git'
    targetRevision: v1.0.0 # Pinning to a specific version tag for production
    path: 3-apps/line-chatbot-service/overlays/production
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: maliev-production
  syncPolicy:
    automated:
      prune: false # Set to true when you are confident
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
