apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: maliev-prod

resources:
  - namespace.yaml
  - ingress.yaml
  - configmap.yaml
  - secrets.yaml
  - ../../3-apps/_common
  - ../_database/base
  - shared-secrets.yaml
  # Infrastructure (DISABLED - uncomment when ready to deploy)
  # - ../../1-cluster-infra/04-redis/base
  # - ../../1-cluster-infra/06-rabbitmq/base

patches:
  - path: db-patch.yaml
    target:
      kind: Cluster
      name: postgres-cluster
