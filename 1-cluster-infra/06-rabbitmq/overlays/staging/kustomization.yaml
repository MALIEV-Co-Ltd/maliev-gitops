apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: maliev-staging

resources:
  - ../../base

# Patch for staging environment
patches:
  - target:
      kind: RabbitmqCluster
      name: maliev-rabbitmq
    patch: |-
      - op: replace
        path: /metadata/name
        value: rabbitmq
      - op: replace
        path: /spec/replicas
        value: 2

  # Update ExternalSecret to use staging-specific secret
  - target:
      kind: ExternalSecret
      name: shared-secrets
    patch: |-
      - op: replace
        path: /spec/dataFrom/0/extract/key
        value: maliev-staging-shared-config

  # Rename alias service to avoid conflict with operator-created service
  # and update selector to point to the new cluster name
  - target:
      kind: Service
      name: rabbitmq
    patch: |-
      - op: replace
        path: /metadata/name
        value: rabbitmq-alias
      - op: replace
        path: /spec/selector/app.kubernetes.io~1name
        value: rabbitmq

labels:
  - pairs:
      environment: staging
