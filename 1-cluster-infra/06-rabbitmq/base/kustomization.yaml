apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - rabbitmq-cluster.yaml    # RabbitMQ Cluster Operator resource
  - service-alias.yaml       # Backward compatibility alias
  - shared-secrets.yaml      # Shared secrets (RabbitMQ credentials) - will be patched per environment

labels:
  - pairs:
      app.kubernetes.io/part-of: maliev
      tier: messaging
