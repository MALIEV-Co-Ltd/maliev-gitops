apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: maliev-infra

resources:
  - rabbitmq-statefulset.yaml

commonLabels:
  managed-by: argocd
  component: rabbitmq
  tier: infrastructure
