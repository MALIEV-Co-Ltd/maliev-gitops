apiVersion: v1
kind: ServiceAccount
metadata:
  name: mssql-backup-ksa
  # This annotation is the key to Workload Identity. It links this KSA
  # to the Google Service Account (GSA) you will create.
  annotations:
    iam.gke.io/gcp-service-account: mssql-backup-gsa@maliev-website.iam.gserviceaccount.com
