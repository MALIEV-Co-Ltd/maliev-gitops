apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-sa
  namespace: external-secrets # ESO is installed in this namespace
  annotations:
    iam.gke.io/gcp-service-account: external-secrets-sa@maliev-website.iam.gserviceaccount.com
