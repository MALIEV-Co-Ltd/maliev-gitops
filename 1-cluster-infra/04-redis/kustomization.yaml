apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: maliev-infra

resources:
  - redis-statefulset.yaml

commonLabels:
  managed-by: argocd
  component: redis
  tier: infrastructure
