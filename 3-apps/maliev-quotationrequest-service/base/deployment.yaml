apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-quotationrequest-service
  labels:
    app: maliev-quotationrequest-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-quotationrequest-service
  template:
    metadata:
      labels:
        app: maliev-quotationrequest-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: maliev-quotationrequest-service
          image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-quotationrequest-service
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: maliev-shared-secrets
            - secretRef:
                name: maliev-quotationrequest-service-secrets
          livenessProbe:
            httpGet:
              path: /quotationrequests/liveness
              port: 8080
          readinessProbe:
            httpGet:
              path: /quotationrequests/readiness
              port: 8080
