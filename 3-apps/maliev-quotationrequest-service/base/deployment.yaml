apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-quotationrequestservice
  labels:
    app: maliev-quotationrequestservice
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-quotationrequestservice
  template:
    metadata:
      labels:
        app: maliev-quotationrequestservice
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-quotationrequestservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-quotationrequestservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /quotationrequests/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /quotationrequests/readiness
            port: 8080
