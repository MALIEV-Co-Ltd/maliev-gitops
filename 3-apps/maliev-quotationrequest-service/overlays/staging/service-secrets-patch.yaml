apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: service-secrets
spec:
  target:
    name: maliev-quotationrequest-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-quotationrequest-service-config
