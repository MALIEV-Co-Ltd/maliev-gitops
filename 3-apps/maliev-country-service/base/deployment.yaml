apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-country-service
  labels:
    app: maliev-country-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-country-service
  template:
    metadata:
      labels:
        app: maliev-country-service
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-country-service
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-country-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /countries/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /countries/readiness
            port: 8080
        envFrom:
        - secretRef:
            name: maliev-shared-secrets
        - secretRef:
            name: maliev-country-service-secrets