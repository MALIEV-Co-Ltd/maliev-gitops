apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: maliev-country-service
  labels:
    app: maliev-country-service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: maliev-country-service
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
