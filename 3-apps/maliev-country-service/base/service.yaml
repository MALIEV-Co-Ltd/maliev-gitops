apiVersion: v1
kind: Service
metadata:
  name: maliev-country-service
  labels:
    app: maliev-country-service
spec:
  type: ClusterIP
  selector:
    app: maliev-country-service
  ports:
    - name: http
      port: 80
      targetPort: 8080