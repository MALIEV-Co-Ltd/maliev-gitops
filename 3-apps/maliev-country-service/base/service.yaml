apiVersion: v1
kind: Service
metadata:
  name: maliev-country-service
  labels:
    app: maliev-country-service
spec:
  type: NodePort
  selector:
    app: maliev-country-service
  ports:
    - port: 8080
      targetPort: 8080