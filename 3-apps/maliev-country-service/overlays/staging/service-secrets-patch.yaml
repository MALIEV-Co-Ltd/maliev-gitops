apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-country-service-secrets
spec:
  target:
    name: maliev-country-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-country-service-config
