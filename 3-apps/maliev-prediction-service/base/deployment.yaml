apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-prediction-service
  labels:
    app: maliev-prediction-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-prediction-service
  template:
    metadata:
      labels:
        app: maliev-prediction-service
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-prediction-service
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-prediction-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /predictions/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /predictions/readiness
            port: 8080
