apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-predictionservice
  labels:
    app: maliev-predictionservice
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-predictionservice
  template:
    metadata:
      labels:
        app: maliev-predictionservice
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-predictionservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-predictionservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /predictions/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /predictions/readiness
            port: 8080
