apiVersion: v1
kind: Service
metadata:
  name: maliev-prediction-service
  labels:
    app: maliev-prediction-service
spec:
  type: ClusterIP
  selector:
    app: maliev-prediction-service
  ports:
  - port: 80
    targetPort: 8080