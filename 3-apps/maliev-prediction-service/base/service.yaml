apiVersion: v1
kind: Service
metadata:
  name: maliev-prediction-service
  labels:
    app: maliev-prediction-service
    monitor: "true"
spec:
  type: NodePort
  selector:
    app: maliev-prediction-service
  ports:
  - port: 8080
    targetPort: 8080