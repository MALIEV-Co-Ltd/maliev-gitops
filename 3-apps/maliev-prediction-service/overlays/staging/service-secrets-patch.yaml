apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-prediction-service-secrets
spec:
  target:
    name: maliev-prediction-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-prediction-service-config
