apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-currency-service-secrets
spec:
  target:
    name: maliev-currency-service-secrets
  dataFrom:
    - extract:
        key: maliev-prod-currency-service-config