apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-currencyservice
  labels:
    app: maliev-currencyservice
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-currencyservice
  template:
    metadata:
      labels:
        app: maliev-currencyservice
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-currencyservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-currencyservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /currencies/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /currencies/readiness
            port: 8080
        envFrom:
        - secretRef:
            name: maliev-shared-secrets
        - secretRef:
            name: maliev-currency-service-secrets
