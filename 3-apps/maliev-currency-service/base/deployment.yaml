apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-currency-service
  labels:
    app: maliev-currency-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-currency-service
  template:
    metadata:
      labels:
        app: maliev-currency-service
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-currency-service
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-currency-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /currencies/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /currencies/readiness
            port: 8080
        envFrom:
        - secretRef:
            name: maliev-shared-secrets
        - secretRef:
            name: maliev-currency-service-secrets
