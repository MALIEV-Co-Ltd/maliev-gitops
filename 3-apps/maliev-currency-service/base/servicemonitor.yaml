apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: maliev-currency-service
  labels:
    app: maliev-currency-service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: maliev-currency-service
  endpoints:
    - port: http
      path: /currency/metrics
      interval: 30s
      scrapeTimeout: 10s