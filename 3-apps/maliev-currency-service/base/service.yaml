apiVersion: v1
kind: Service
metadata:
  name: maliev-currency-service
  labels:
    app: maliev-currency-service
spec:
  type: NodePort
  selector:
    app: maliev-currency-service
  ports:
  - port: 8080
    targetPort: 8080