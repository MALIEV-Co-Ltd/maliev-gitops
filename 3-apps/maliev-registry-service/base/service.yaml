apiVersion: v1
kind: Service
metadata:
  name: maliev-registry-service
  labels:
    app: maliev-registry-service
spec:
  type: NodePort
  selector:
    app: maliev-registry-service
  ports:
    - port: 8080
      targetPort: 8080
