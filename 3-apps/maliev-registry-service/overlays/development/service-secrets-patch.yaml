apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-registry-service-secrets
spec:
  target:
    name: maliev-registry-service-secrets
  dataFrom:
    - extract:
        key: maliev-dev-registry-service-config
