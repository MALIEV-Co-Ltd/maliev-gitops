apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-registry-service
spec:
  template:
    spec:
      containers:
        - name: maliev-registry-service
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Staging"
