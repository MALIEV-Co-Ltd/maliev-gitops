apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-material-service
  labels:
    app: maliev-material-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-material-service
  template:
    metadata:
      labels:
        app: maliev-material-service
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-material-service
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-material-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /materials/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /materials/readiness
            port: 8080
        envFrom:
        - secretRef:
            name: maliev-material-service-secrets
