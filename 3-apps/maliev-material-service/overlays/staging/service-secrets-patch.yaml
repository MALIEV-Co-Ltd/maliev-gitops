apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: service-secrets
spec:
  target:
    name: maliev-material-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-material-service-config
