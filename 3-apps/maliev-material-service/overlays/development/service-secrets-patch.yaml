apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-material-service-secrets
spec:
  target:
    name: maliev-material-service-secrets
  dataFrom:
  - extract:
      key: maliev-dev-material-service-config
