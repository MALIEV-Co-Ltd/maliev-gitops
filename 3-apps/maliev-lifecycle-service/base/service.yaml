apiVersion: v1
kind: Service
metadata:
  name: maliev-lifecycle-service
spec:
  selector:
    app: maliev-lifecycle-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
