# Legacy MALIEV Career Service

This directory contains the dormant `maliev-legacy` runtime secret projection for
`Legacy.Maliev.CareerService`. It is deliberately excluded from the active
environment kustomization until local Aspire validation and the owner release gate
are complete.
