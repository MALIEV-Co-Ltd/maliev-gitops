apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: service-secrets
spec:
  target:
    name: maliev-receipt-service-secrets
  dataFrom:
  - extract:
      key: maliev-dev-receipt-service-config
