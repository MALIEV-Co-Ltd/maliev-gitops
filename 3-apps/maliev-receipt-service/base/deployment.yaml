apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-receipt-service
  labels:
    app: maliev-receipt-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-receipt-service
  template:
    metadata:
      labels:
        app: maliev-receipt-service
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-receipt-service
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-receipt-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /receipts/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /receipts/readiness
            port: 8080
