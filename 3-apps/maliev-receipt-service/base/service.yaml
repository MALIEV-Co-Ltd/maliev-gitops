apiVersion: v1
kind: Service
metadata:
  name: maliev-receiptservice
  labels:
    app: maliev-receiptservice
spec:
  type: ClusterIP
  selector:
    app: maliev-receiptservice
  ports:
  - port: 80
    targetPort: 8080