apiVersion: v1
kind: Service
metadata:
  name: maliev-receipt-service
  labels:
    app: maliev-receipt-service
spec:
  type: ClusterIP
  selector:
    app: maliev-receipt-service
  ports:
  - port: 80
    targetPort: 8080