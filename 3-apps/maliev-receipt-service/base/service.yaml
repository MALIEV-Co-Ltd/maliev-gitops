apiVersion: v1
kind: Service
metadata:
  name: maliev-receipt-service
  labels:
    app: maliev-receipt-service
    monitor: "true"
spec:
  type: NodePort
  selector:
    app: maliev-receipt-service
  ports:
  - port: 8080
    targetPort: 8080