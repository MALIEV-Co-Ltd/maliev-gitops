apiVersion: v1
kind: Service
metadata:
  name: maliev-leave-service
spec:
  selector:
    app: maliev-leave-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
