apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-uploadservice
spec:
  template:
    spec:
      
      containers:
      - name: maliev-uploadservice
        livenessProbe:
          httpGet:
            path: /uploads/liveness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /uploads/readiness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Staging"
        - name: ConnectionStrings__UploadDbContext
          valueFrom:
            secretKeyRef:
              name: upload-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-string