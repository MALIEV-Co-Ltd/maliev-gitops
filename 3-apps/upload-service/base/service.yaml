apiVersion: v1
kind: Service
metadata:
  name: maliev-uploadservice-api
  labels:
    app: maliev-uploadservice-api
spec:
  type: ClusterIP
  selector:
    app: maliev-uploadservice-api
  ports:
  - port: 80
    targetPort: 8080