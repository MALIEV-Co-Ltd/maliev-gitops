apiVersion: v1
kind: Service
metadata:
  name: maliev-uploadservice
  labels:
    app: maliev-uploadservice
spec:
  type: ClusterIP
  selector:
    app: maliev-uploadservice
  ports:
  - port: 80
    targetPort: 8080