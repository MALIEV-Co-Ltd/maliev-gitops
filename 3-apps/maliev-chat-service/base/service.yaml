apiVersion: v1
kind: Service
metadata:
  name: maliev-chat-service
  namespace: maliev
  labels:
    run: maliev-chat-service
spec:
  ports:
    - port: 80
      targetPort: 8000
      name: http
  selector:
    app: maliev-chat-service
  sessionAffinity: None
  type: NodePort
