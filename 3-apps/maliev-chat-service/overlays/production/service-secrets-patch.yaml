apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-chat-service-secrets
spec:
  target:
    name: maliev-chat-service-secrets
  dataFrom:
    - extract:
        key: maliev-prod-chat-service-config
