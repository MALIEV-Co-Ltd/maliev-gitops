apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-pdf-service-secrets
spec:
  target:
    name: maliev-pdf-service-secrets
  dataFrom:
  - extract:
      key: maliev-dev-pdf-service-config
