apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-pdf-service
  labels:
    app: maliev-pdf-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-pdf-service
  template:
    metadata:
      labels:
        app: maliev-pdf-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: maliev-pdf-service
          image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-pdf-service
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /pdfs/liveness
              port: 8080
          readinessProbe:
            httpGet:
              path: /pdfs/readiness
              port: 8080
