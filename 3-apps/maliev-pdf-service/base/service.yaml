apiVersion: v1
kind: Service
metadata:
  name: maliev-pdf-service
  labels:
    app: maliev-pdf-service
    monitor: "true"
spec:
  type: NodePort
  selector:
    app: maliev-pdf-service
  ports:
  - port: 8080
    targetPort: 8080