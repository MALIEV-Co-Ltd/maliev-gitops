apiVersion: v1
kind: Service
metadata:
  name: maliev-pdf-service
  labels:
    app: maliev-pdf-service
spec:
  type: ClusterIP
  selector:
    app: maliev-pdf-service
  ports:
  - port: 80
    targetPort: 8080