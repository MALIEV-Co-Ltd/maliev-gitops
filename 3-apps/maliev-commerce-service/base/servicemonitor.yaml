apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: maliev-commerce-service
  labels:
    app: maliev-commerce-service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: maliev-commerce-service
  endpoints:
    - port: http
      path: /commerce/metrics
      interval: 30s
      scrapeTimeout: 10s
