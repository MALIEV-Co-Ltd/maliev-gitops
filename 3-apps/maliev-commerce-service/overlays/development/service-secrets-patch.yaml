apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-commerce-service-secrets
spec:
  target:
    name: maliev-commerce-service-secrets
  dataFrom:
    - extract:
        key: maliev-dev-commerce-service-config
