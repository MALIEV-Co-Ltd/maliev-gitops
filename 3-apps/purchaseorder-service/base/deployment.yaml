apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-purchaseorderservice
  labels:
    app: maliev-purchaseorderservice
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-purchaseorderservice
  template:
    metadata:
      labels:
        app: maliev-purchaseorderservice
    spec:
      nodeSelector:
        cloud.google.com/gke-nodepool: backend-pool
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-purchaseorderservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-purchaseorderservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /purchaseorders/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /purchaseorders/readiness
            port: 8080
