apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-purchaseorderservice-api
  labels:
    app: maliev-purchaseorderservice-api
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-purchaseorderservice-api
  template:
    metadata:
      labels:
        app: maliev-purchaseorderservice-api
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-purchaseorderservice-api
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev.purchaseorderservice.api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /purchaseorders/liveness
            port: 8080
          initialDelaySeconds: 45
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /purchaseorders/readiness
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        env:
        - name: ConnectionStrings__PurchaseOrderDbContext
          valueFrom:
            secretKeyRef:
              name: purchaseorder-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-string