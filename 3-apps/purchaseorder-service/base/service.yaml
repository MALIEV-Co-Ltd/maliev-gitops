apiVersion: v1
kind: Service
metadata:
  name: maliev-purchaseorderservice-api
  labels:
    app: maliev-purchaseorderservice-api
spec:
  type: ClusterIP
  selector:
    app: maliev-purchaseorderservice-api
  ports:
  - port: 80
    targetPort: 8080