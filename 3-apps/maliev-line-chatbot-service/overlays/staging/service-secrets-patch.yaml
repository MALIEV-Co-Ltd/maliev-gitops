apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-line-chatbot-service-secrets
spec:
  target:
    name: maliev-line-chatbot-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-line-chatbot-service-config
