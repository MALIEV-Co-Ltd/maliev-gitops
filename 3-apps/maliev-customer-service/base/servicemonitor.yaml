apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: maliev-customer-service
  labels:
    app: maliev-customer-service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: maliev-customer-service
  endpoints:
    - port: http
      path: /customer/metrics
      interval: 30s
      scrapeTimeout: 10s
