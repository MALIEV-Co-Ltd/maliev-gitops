apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-customer-service
  labels:
    app: maliev-customer-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-customer-service
  template:
    metadata:
      labels:
        app: maliev-customer-service
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-customer-service
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-customer-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /customers/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /customers/readiness
            port: 8080
        envFrom:
        - secretRef:
            name: maliev-shared-secrets
        - secretRef:
            name: maliev-customer-service-secrets
