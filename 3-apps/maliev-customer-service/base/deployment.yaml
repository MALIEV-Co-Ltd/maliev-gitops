apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-customer-service
  labels:
    app: maliev-customer-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-customer-service
  template:
    metadata:
      labels:
        app: maliev-customer-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: maliev-customer-service
          image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-customer-service:latest
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 3000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          imagePullPolicy: Always
          resources:
            requests:
              cpu: "10m"
              memory: "96Mi"
            limits:
              cpu: "25m"
              memory: "128Mi"
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /customers/liveness
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /customers/readiness
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
            successThreshold: 1
          envFrom:
            - secretRef:
                name: maliev-shared-secrets
            - secretRef:
                name: maliev-customer-service-secrets