apiVersion: v1
kind: Service
metadata:
  name: maliev-customerservice
  labels:
    app: maliev-customerservice
spec:
  type: ClusterIP
  selector:
    app: maliev-customerservice
  ports:
  - port: 80
    targetPort: 8080