apiVersion: v1
kind: Service
metadata:
  name: maliev-customer-service
  labels:
    app: maliev-customer-service
spec:
  type: ClusterIP
  selector:
    app: maliev-customer-service
  ports:
  - port: 80
    targetPort: 8080