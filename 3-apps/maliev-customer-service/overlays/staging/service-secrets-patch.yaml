apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-customer-service-secrets
spec:
  target:
    name: maliev-customer-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-customer-service-config
