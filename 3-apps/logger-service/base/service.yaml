apiVersion: v1
kind: Service
metadata:
  name: maliev-loggerservice
  labels:
    app: maliev-loggerservice
spec:
  type: ClusterIP
  selector:
    app: maliev-loggerservice
  ports:
  - port: 80
    targetPort: 8080