apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-loggerservice
  labels:
    app: maliev-loggerservice
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-loggerservice
  template:
    metadata:
      labels:
        app: maliev-loggerservice
    spec:
      nodeSelector:
        cloud.google.com/gke-nodepool: backend-pool
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-loggerservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-loggerservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /logs/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /logs/readiness
            port: 8080
