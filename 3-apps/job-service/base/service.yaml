apiVersion: v1
kind: Service
metadata:
  name: maliev-jobservice-api
  labels:
    app: maliev-jobservice-api
spec:
  type: ClusterIP
  selector:
    app: maliev-jobservice-api
  ports:
  - port: 80
    targetPort: 8080