apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-jobservice
  labels:
    app: maliev-jobservice
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-jobservice
  template:
    metadata:
      labels:
        app: maliev-jobservice
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-jobservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev.jobservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /jobs/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /jobs/readiness
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        env:
        - name: ConnectionStrings__JobsDbContext
          valueFrom:
            secretKeyRef:
              name: jobs-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-stringonnection-string