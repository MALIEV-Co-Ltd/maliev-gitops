apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-jobservice
spec:
  template:
    spec:
      nodeSelector:
        cloud.google.com/gke-nodepool: backend-pool
      containers:
      - name: maliev-jobservice
        livenessProbe:
          httpGet:
            path: /jobs/liveness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /jobs/readiness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
