apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-log-service
  labels:
    app: maliev-log-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-log-service
  template:
    metadata:
      labels:
        app: maliev-log-service
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-log-service
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-log-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /logs/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /logs/readiness
            port: 8080
