apiVersion: v1
kind: Service
metadata:
  name: maliev-log-service
  labels:
    app: maliev-log-service
spec:
  type: NodePort
  selector:
    app: maliev-log-service
  ports:
    - port: 8080
      targetPort: 8080
