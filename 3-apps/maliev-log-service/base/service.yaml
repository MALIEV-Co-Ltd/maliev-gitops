apiVersion: v1
kind: Service
metadata:
  name: maliev-log-service
  labels:
    app: maliev-log-service
spec:
  type: ClusterIP
  selector:
    app: maliev-log-service
  ports:
  - port: 80
    targetPort: 8080