apiVersion: v1
kind: Service
metadata:
  name: maliev-logservice
  labels:
    app: maliev-logservice
spec:
  type: ClusterIP
  selector:
    app: maliev-logservice
  ports:
  - port: 80
    targetPort: 8080