apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-log-service-secrets
spec:
  target:
    name: maliev-log-service-secrets
  dataFrom:
    - extract:
        key: maliev-staging-log-service-config
