apiVersion: v1
kind: Service
metadata:
  name: maliev-upload-service
  labels:
    app: maliev-upload-service
spec:
  type: NodePort
  selector:
    app: maliev-upload-service
  ports:
  - port: 80
    targetPort: 8080