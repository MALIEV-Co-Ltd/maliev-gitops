apiVersion: v1
kind: Service
metadata:
  name: maliev-upload-service
  labels:
    app: maliev-upload-service
    monitor: "true"
spec:
  type: NodePort
  selector:
    app: maliev-upload-service
  ports:
  - port: 8080
    targetPort: 8080