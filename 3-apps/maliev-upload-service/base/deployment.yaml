apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-upload-service
  labels:
    app: maliev-upload-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-upload-service
  template:
    metadata:
      labels:
        app: maliev-upload-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: maliev-upload-service
          envFrom:
            - secretRef:
                name: maliev-shared-secrets
            - secretRef:
                name: maliev-upload-service-secrets
          image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-upload-service
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /uploads/liveness
              port: 8080
          readinessProbe:
            httpGet:
              path: /uploads/readiness
              port: 8080
