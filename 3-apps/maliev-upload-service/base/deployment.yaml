apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-uploadservice
  labels:
    app: maliev-uploadservice
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-uploadservice
  template:
    metadata:
      labels:
        app: maliev-uploadservice
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-uploadservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-uploadservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /uploads/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /uploads/readiness
            port: 8080
