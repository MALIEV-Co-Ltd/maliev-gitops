apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-upload-service-secrets
spec:
  target:
    name: maliev-upload-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-upload-service-config
