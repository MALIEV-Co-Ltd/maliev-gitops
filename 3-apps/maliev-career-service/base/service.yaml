apiVersion: v1
kind: Service
metadata:
  name: maliev-career-service
  labels:
    app: maliev-career-service
    monitor: "true"
spec:
  type: NodePort
  selector:
    app: maliev-career-service
  ports:
  - port: 8080
    targetPort: 8080