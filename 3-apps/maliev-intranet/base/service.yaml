apiVersion: v1
kind: Service
metadata:
  name: maliev-intranet
  labels:
    app: maliev-intranet
spec:
  type: NodePort
  selector:
    app: maliev-intranet
  ports:
  - port: 80
    targetPort: 8080