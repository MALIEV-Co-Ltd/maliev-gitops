apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-intranet-secrets
spec:
  target:
    name: maliev-intranet-secrets
  dataFrom:
  - extract:
      key: maliev-staging-intranet-config
