apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: service-secrets
spec:
  target:
    name: maliev-intranet-secrets
  dataFrom:
  - extract:
      key: maliev-dev-intranet-config
