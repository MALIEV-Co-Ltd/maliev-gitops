apiVersion: v1
kind: Service
metadata:
  name: maliev-compliance-service
spec:
  selector:
    app: maliev-compliance-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
