apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-orderstatusservice
  labels:
    app: maliev-orderstatusservice
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-orderstatusservice
  template:
    metadata:
      labels:
        app: maliev-orderstatusservice
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-orderstatusservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-orderstatusservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /orderstatuses/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /orderstatuses/readiness
            port: 8080
