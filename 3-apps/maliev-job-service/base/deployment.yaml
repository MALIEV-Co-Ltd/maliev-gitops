apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-job-service
  labels:
    app: maliev-job-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-job-service
  template:
    metadata:
      labels:
        app: maliev-job-service
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-job-service
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-job-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /jobs/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /jobs/readiness
            port: 8080
