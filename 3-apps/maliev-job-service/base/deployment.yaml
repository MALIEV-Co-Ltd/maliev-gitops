apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-career-service
  labels:
    app: maliev-career-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-career-service
  template:
    metadata:
      labels:
        app: maliev-career-service
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-career-service
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-career-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /careers/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /careers/readiness
            port: 8080
