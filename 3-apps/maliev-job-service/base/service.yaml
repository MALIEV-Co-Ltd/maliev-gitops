apiVersion: v1
kind: Service
metadata:
  name: maliev-jobservice
  labels:
    app: maliev-jobservice
spec:
  type: ClusterIP
  selector:
    app: maliev-jobservice
  ports:
  - port: 80
    targetPort: 8080