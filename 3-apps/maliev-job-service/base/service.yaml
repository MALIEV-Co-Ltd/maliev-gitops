apiVersion: v1
kind: Service
metadata:
  name: maliev-job-service
  labels:
    app: maliev-job-service
spec:
  type: NodePort
  selector:
    app: maliev-job-service
  ports:
    - port: 8080
      targetPort: 8080
