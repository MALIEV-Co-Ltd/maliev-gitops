apiVersion: v1
kind: Service
metadata:
  name: maliev-job-service
  labels:
    app: maliev-job-service
spec:
  type: ClusterIP
  selector:
    app: maliev-job-service
  ports:
  - port: 80
    targetPort: 8080