apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: service-secrets
spec:
  target:
    name: maliev-job-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-job-service-config
