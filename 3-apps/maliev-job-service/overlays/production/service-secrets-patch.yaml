apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-career-service-secrets
spec:
  target:
    name: maliev-career-service-secrets
  dataFrom:
  - extract:
      key: maliev-prod-career-service-config
