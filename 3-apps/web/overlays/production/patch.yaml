apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-web-deployment
spec:
  template:
    spec:
      containers:
      - name: web-container
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 500m
            memory: 1Gi
