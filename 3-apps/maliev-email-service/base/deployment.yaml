apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-email-service
  labels:
    app: maliev-email-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-email-service
  template:
    metadata:
      labels:
        app: maliev-email-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: maliev-email-service
          image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-email-service
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /emails/liveness
              port: 8080
          readinessProbe:
            httpGet:
              path: /emails/readiness
              port: 8080
          envFrom:
            - secretRef:
                name: maliev-shared-secrets
            - secretRef:
                name: maliev-email-service-secrets