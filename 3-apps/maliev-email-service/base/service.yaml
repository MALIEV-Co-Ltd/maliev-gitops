apiVersion: v1
kind: Service
metadata:
  name: maliev-email-service
  labels:
    app: maliev-email-service
    monitor: "true"
spec:
  type: NodePort
  selector:
    app: maliev-email-service
  ports:
  - port: 8080
    targetPort: 8080