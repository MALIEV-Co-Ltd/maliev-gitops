apiVersion: v1
kind: Service
metadata:
  name: maliev-email-service
  labels:
    app: maliev-email-service
spec:
  type: ClusterIP
  selector:
    app: maliev-email-service
  ports:
  - port: 80
    targetPort: 8080