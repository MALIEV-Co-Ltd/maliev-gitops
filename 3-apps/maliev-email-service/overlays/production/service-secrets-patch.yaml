apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-email-service-secrets
spec:
  target:
    name: maliev-email-service-secrets
  dataFrom:
  - extract:
      key: maliev-prod-email-service-config
