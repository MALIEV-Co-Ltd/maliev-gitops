apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: service-secrets
spec:
  target:
    name: maliev-email-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-email-service-config
