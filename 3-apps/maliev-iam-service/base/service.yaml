apiVersion: v1
kind: Service
metadata:
  name: maliev-iam-service
  labels:
    app: maliev-iam-service
spec:
  type: NodePort
  selector:
    app: maliev-iam-service
  ports:
    - port: 8080
      targetPort: 8080
