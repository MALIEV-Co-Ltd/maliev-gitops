apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-iam-service
  labels:
    app: maliev-iam-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-iam-service
  template:
    metadata:
      labels:
        app: maliev-iam-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: maliev-iam-service
          image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-iam-service
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: maliev-shared-secrets
            - secretRef:
                name: maliev-iam-service-secrets
          livenessProbe:
            httpGet:
              path: /iam/liveness
              port: 8080
          readinessProbe:
            httpGet:
              path: /iam/readiness
              port: 8080
