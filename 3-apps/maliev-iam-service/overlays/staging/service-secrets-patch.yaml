apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-iam-service-secrets
spec:
  target:
    name: maliev-iam-service-secrets
  dataFrom:
    - extract:
        key: maliev-staging-iam-service-config
