apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-quotationservice
  labels:
    app: maliev-quotationservice
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-quotationservice
  template:
    metadata:
      labels:
        app: maliev-quotationservice
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-quotationservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev.quotationservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /quotations/liveness
            port: 8080
          initialDelaySeconds: 45
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /quotations/readiness
            port: 8080
        env:
        - name: ConnectionStrings__QuotationDbContext
          valueFrom:
            secretKeyRef:
              name: quotation-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-stringonnection-string