apiVersion: v1
kind: Service
metadata:
  name: maliev-quotationservice
  labels:
    app: maliev-quotationservice
spec:
  type: ClusterIP
  selector:
    app: maliev-quotationservice
  ports:
  - port: 80
    targetPort: 8080