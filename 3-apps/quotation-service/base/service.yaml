apiVersion: v1
kind: Service
metadata:
  name: maliev-quotationservice-api
  labels:
    app: maliev-quotationservice-api
spec:
  type: ClusterIP
  selector:
    app: maliev-quotationservice-api
  ports:
  - port: 80
    targetPort: 8080