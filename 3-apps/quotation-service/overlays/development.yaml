apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-quotationservice
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: maliev-quotationservice
        livenessProbe:
          httpGet:
            path: /quotations/liveness
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /quotations/readiness
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3