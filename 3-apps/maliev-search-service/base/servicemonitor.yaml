apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: maliev-search-service
  labels:
    app: maliev-search-service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: maliev-search-service
  endpoints:
    - port: http
      path: /search/metrics
      interval: 30s
      scrapeTimeout: 10s
