apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-purchaseorder-service-secrets
spec:
  target:
    name: maliev-purchaseorder-service-secrets
  dataFrom:
  - extract:
      key: maliev-dev-purchaseorder-service-config
