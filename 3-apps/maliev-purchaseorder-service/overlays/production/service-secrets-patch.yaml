apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: service-secrets
spec:
  target:
    name: maliev-purchaseorder-service-secrets
  dataFrom:
  - extract:
      key: maliev-prod-purchaseorder-service-config
