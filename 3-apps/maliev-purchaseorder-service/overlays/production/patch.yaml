apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-purchaseorder-service-deployment
spec:
  template:
    spec:
      containers:
      - name: purchaseorder-service-container
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 500m
            memory: 1Gi
