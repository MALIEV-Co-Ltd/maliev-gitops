apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: maliev-purchaseorder-service
  labels:
    app: maliev-purchaseorder-service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: maliev-purchaseorder-service
  endpoints:
    - port: http
      path: /purchaseorder/metrics
      interval: 30s
      scrapeTimeout: 10s