apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-purchaseorder-service
  labels:
    app: maliev-purchaseorder-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-purchaseorder-service
  template:
    metadata:
      labels:
        app: maliev-purchaseorder-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: maliev-purchaseorder-service
          image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-purchaseorder-service
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: maliev-shared-secrets
            - secretRef:
                name: maliev-purchaseorder-service-secrets
          livenessProbe:
            httpGet:
              path: /purchaseorders/liveness
              port: 8080
          readinessProbe:
            httpGet:
              path: /purchaseorders/readiness
              port: 8080
