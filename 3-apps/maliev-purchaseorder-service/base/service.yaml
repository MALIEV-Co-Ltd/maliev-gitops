apiVersion: v1
kind: Service
metadata:
  name: maliev-purchaseorderservice
  labels:
    app: maliev-purchaseorderservice
spec:
  type: ClusterIP
  selector:
    app: maliev-purchaseorderservice
  ports:
  - port: 80
    targetPort: 8080