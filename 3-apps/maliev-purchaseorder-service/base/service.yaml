apiVersion: v1
kind: Service
metadata:
  name: maliev-purchaseorder-service
  labels:
    app: maliev-purchaseorder-service
spec:
  type: NodePort
  selector:
    app: maliev-purchaseorder-service
  ports:
  - port: 80
    targetPort: 8080