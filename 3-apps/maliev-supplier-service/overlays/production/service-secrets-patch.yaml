apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-supplier-service-secrets
spec:
  target:
    name: maliev-supplier-service-secrets
  dataFrom:
  - extract:
      key: maliev-prod-supplier-service-config
