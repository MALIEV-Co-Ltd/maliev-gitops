apiVersion: v1
kind: Service
metadata:
  name: maliev-supplier-service
  labels:
    app: maliev-supplier-service
    monitor: "true"
spec:
  type: NodePort
  selector:
    app: maliev-supplier-service
  ports:
  - port: 8080
    targetPort: 8080