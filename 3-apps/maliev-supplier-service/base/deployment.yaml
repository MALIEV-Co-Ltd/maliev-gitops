apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-supplier-service
  labels:
    app: maliev-supplier-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-supplier-service
  template:
    metadata:
      labels:
        app: maliev-supplier-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: maliev-supplier-service
          image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-supplier-service
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: maliev-shared-secrets
            - secretRef:
                name: maliev-supplier-service-secrets
          livenessProbe:
            httpGet:
              path: /suppliers/liveness
              port: 8080
          readinessProbe:
            httpGet:
              path: /suppliers/readiness
              port: 8080
