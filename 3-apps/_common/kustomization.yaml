# This file can be used to apply common labels or annotations to all applications.
# For example, you could add a label that identifies your company or team.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
- pairs:
    app.kubernetes.io/part-of: maliev
