apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-geometry-service-secrets
spec:
  target:
    name: maliev-geometry-service-secrets
  dataFrom:
    - extract:
        key: maliev-staging-geometry-service-config
