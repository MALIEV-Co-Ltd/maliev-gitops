apiVersion: v1
kind: Service
metadata:
  name: maliev-geometry-service
  labels:
    app: maliev-geometry-service
spec:
  type: NodePort
  selector:
    app: maliev-geometry-service
  ports:
    - port: 8080
      targetPort: 8080
