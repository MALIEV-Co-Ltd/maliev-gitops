apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: service-secrets
spec:
  target:
    name: maliev-invoice-service-secrets
  dataFrom:
  - extract:
      key: maliev-prod-invoice-service-config
