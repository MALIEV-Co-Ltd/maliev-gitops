apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-invoice-service-secrets
spec:
  target:
    name: maliev-invoice-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-invoice-service-config
