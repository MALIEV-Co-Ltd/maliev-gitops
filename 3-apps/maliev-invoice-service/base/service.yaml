apiVersion: v1
kind: Service
metadata:
  name: maliev-invoice-service
  labels:
    app: maliev-invoice-service
spec:
  type: ClusterIP
  selector:
    app: maliev-invoice-service
  ports:
  - port: 80
    targetPort: 8080