apiVersion: v1
kind: Service
metadata:
  name: maliev-invoice-service
  labels:
    app: maliev-invoice-service
    monitor: "true"
spec:
  type: NodePort
  selector:
    app: maliev-invoice-service
  ports:
  - port: 8080
    targetPort: 8080