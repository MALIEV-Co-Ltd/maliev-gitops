apiVersion: v1
kind: Service
metadata:
  name: maliev-invoiceservice
  labels:
    app: maliev-invoiceservice
spec:
  type: ClusterIP
  selector:
    app: maliev-invoiceservice
  ports:
  - port: 80
    targetPort: 8080