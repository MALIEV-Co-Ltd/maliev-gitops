apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-invoice-service
  labels:
    app: maliev-invoice-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-invoice-service
  template:
    metadata:
      labels:
        app: maliev-invoice-service
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-invoice-service
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-invoice-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /invoices/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /invoices/readiness
            port: 8080
