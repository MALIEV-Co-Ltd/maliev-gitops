apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-project-service
spec:
  template:
    spec:
      containers:
        - name: maliev-project-service
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Development"
