apiVersion: v1
kind: Secret
metadata:
  name: maliev-project-service-secrets
type: Opaque
stringData:
  # Add service-specific secrets here
  # Examples:
  # ConnectionStrings__DefaultConnection: "postgres://..."
  # ExternalServices__PricingServiceUrl: "http://pricing-service"
  # ExternalServices__QuotationServiceUrl: "http://quotation-service"
