apiVersion: v1
kind: Service
metadata:
  name: maliev-project-service
  labels:
    app: maliev-project-service
spec:
  type: ClusterIP
  selector:
    app: maliev-project-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
