apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-customerservice-api
  labels:
    app: maliev-customerservice-api
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-customerservice-api
  template:
    metadata:
      labels:
        app: maliev-customerservice-api
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-customerservice-api
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev.customerservice.api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /customers/liveness
            port: 8080
          initialDelaySeconds: 45
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /customers/readiness
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        env:
        - name: ConnectionStrings__CustomerIdentityDbContext
          valueFrom:
            secretKeyRef:
              name: customer-identity-db-conn
              key: connection-string
        - name: ConnectionStrings__CustomerDbContext
          valueFrom:
            secretKeyRef:
              name: customer-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-string