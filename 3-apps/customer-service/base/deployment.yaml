apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-customerservice
  labels:
    app: maliev-customerservice
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-customerservice
  template:
    metadata:
      labels:
        app: maliev-customerservice
    spec:
      nodeSelector:
        cloud.google.com/gke-nodepool: backend-pool
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-customerservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-customerservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /customers/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /customers/readiness
            port: 8080
