apiVersion: v1
kind: Service
metadata:
  name: maliev-facility-service
  labels:
    app: maliev-facility-service
spec:
  type: NodePort
  selector:
    app: maliev-facility-service
  ports:
    - port: 8080
      targetPort: 8080
