apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-facility-service-secrets
spec:
  target:
    name: maliev-facility-service-secrets
  dataFrom:
    - extract:
        key: maliev-staging-facility-service-config
