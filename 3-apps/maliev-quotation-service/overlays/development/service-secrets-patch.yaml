apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-quotation-service-secrets
spec:
  target:
    name: maliev-quotation-service-secrets
  dataFrom:
  - extract:
      key: maliev-dev-quotation-service-config
