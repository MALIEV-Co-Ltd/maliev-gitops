apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: service-secrets
spec:
  target:
    name: maliev-quotation-service-secrets
  dataFrom:
  - extract:
      key: maliev-prod-quotation-service-config
