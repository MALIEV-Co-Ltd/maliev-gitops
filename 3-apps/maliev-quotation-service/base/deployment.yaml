apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-quotation-service
  labels:
    app: maliev-quotation-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-quotation-service
  template:
    metadata:
      labels:
        app: maliev-quotation-service
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-quotation-service
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-quotation-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /quotations/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /quotations/readiness
            port: 8080
