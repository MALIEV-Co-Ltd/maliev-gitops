apiVersion: v1
kind: Service
metadata:
  name: maliev-quotation-service
  labels:
    app: maliev-quotation-service
spec:
  type: NodePort
  selector:
    app: maliev-quotation-service
  ports:
    - port: 8080
      targetPort: 8080
