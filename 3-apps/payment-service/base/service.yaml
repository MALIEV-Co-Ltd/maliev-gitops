apiVersion: v1
kind: Service
metadata:
  name: maliev-paymentservice-api
  labels:
    app: maliev-paymentservice-api
spec:
  type: ClusterIP
  selector:
    app: maliev-paymentservice-api
  ports:
  - port: 80
    targetPort: 8080