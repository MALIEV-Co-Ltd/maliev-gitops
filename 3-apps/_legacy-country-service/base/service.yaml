apiVersion: v1
kind: Service
metadata:
  name: legacy-maliev-country-service
  labels:
    app.kubernetes.io/name: legacy-maliev-country-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: legacy-maliev-country-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
