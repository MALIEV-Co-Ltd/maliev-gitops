apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-accounting-service-secrets
spec:
  target:
    name: maliev-accounting-service-secrets
  dataFrom:
    - extract:
        key: maliev-staging-accounting-service-config
