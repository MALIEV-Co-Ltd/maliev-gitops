apiVersion: v1
kind: Service
metadata:
  name: maliev-accounting-service
  labels:
    app: maliev-accounting-service
spec:
  type: NodePort
  selector:
    app: maliev-accounting-service
  ports:
    - port: 8080
      targetPort: 8080
