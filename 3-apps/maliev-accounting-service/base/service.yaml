apiVersion: v1
kind: Service
metadata:
  name: maliev-accounting-service
  labels:
    app: maliev-accounting-service
spec:
  type: ClusterIP
  selector:
    app: maliev-accounting-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
