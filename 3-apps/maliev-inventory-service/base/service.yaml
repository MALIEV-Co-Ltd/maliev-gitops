apiVersion: v1
kind: Service
metadata:
  name: maliev-inventory-service
  labels:
    app: maliev-inventory-service
spec:
  type: NodePort
  selector:
    app: maliev-inventory-service
  ports:
    - port: 8080
      targetPort: 8080
