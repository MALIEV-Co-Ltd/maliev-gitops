apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-payment-service-secrets
spec:
  target:
    name: maliev-payment-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-payment-service-config
