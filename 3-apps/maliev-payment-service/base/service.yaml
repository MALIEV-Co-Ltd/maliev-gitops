apiVersion: v1
kind: Service
metadata:
  name: maliev-payment-service
  labels:
    app: maliev-payment-service
spec:
  type: NodePort
  selector:
    app: maliev-payment-service
  ports:
  - port: 8080
    targetPort: 8080