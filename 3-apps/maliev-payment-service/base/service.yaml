apiVersion: v1
kind: Service
metadata:
  name: maliev-paymentservice
  labels:
    app: maliev-paymentservice
spec:
  type: ClusterIP
  selector:
    app: maliev-paymentservice
  ports:
  - port: 80
    targetPort: 8080