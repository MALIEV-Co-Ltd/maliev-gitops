apiVersion: v1
kind: Service
metadata:
  name: maliev-payment-service
  labels:
    app: maliev-payment-service
spec:
  type: ClusterIP
  selector:
    app: maliev-payment-service
  ports:
  - port: 80
    targetPort: 8080