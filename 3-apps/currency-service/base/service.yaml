apiVersion: v1
kind: Service
metadata:
  name: maliev-currencyservice
  labels:
    app: maliev-currencyservice
spec:
  type: ClusterIP
  selector:
    app: maliev-currencyservice
  ports:
  - port: 80
    targetPort: 8080