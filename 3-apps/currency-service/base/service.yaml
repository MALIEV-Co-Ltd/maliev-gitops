apiVersion: v1
kind: Service
metadata:
  name: maliev-currencyservice-api
  labels:
    app: maliev-currencyservice-api
spec:
  type: ClusterIP
  selector:
    app: maliev-currencyservice-api
  ports:
  - port: 80
    targetPort: 8080