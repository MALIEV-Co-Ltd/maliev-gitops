apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-notification-service
spec:
  template:
    spec:
      containers:
        - name: maliev-notification-service
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"