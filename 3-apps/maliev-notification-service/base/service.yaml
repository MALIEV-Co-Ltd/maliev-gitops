apiVersion: v1
kind: Service
metadata:
  name: maliev-notification-service
spec:
  selector:
    app: maliev-notification-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP