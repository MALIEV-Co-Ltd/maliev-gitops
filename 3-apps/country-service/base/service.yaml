apiVersion: v1
kind: Service
metadata:
  name: maliev-countryservice
  labels:
    app: maliev-countryservice
spec:
  type: ClusterIP
  selector:
    app: maliev-countryservice
  ports:
  - port: 80
    targetPort: 8080