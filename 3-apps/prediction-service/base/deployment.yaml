apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-predictionservice-api
  labels:
    app: maliev-predictionservice-api
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-predictionservice-api
  template:
    metadata:
      labels:
        app: maliev-predictionservice-api
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-predictionservice-api
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev.predictionservice.api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /predictions/liveness
            port: 8080
          initialDelaySeconds: 45
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /predictions/readiness
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        env:
        - name: ConnectionStrings__HangfireDatabase
          valueFrom:
            secretKeyRef:
              name: hangfire-db-conn
              key: connection-string
        - name: ConnectionStrings__MachineLearningDataDbContext
          valueFrom:
            secretKeyRef:
              name: ml-data-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-string