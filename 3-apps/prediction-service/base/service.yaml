apiVersion: v1
kind: Service
metadata:
  name: maliev-predictionservice
  labels:
    app: maliev-predictionservice
spec:
  type: ClusterIP
  selector:
    app: maliev-predictionservice
  ports:
  - port: 80
    targetPort: 8080