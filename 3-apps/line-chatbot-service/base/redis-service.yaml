apiVersion: v1
kind: Service
metadata:
  name: line-chatbot-service-redis
  
spec:
  type: ClusterIP
  selector:
    app: line-chatbot-service-redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379