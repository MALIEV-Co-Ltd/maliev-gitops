apiVersion: v1
kind: Service
metadata:
  name: maliev-line-chatbot-redis
  
spec:
  type: ClusterIP
  selector:
    app: maliev-line-chatbot-redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379