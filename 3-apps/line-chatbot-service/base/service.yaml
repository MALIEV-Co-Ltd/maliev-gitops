apiVersion: v1
kind: Service
metadata:
  name: maliev-line-chatbot-service
  namespace: maliev
  labels:
    run: maliev-line-chatbot-service
spec:
  ports:
  - port: 80
    targetPort: 8080
    name: http
  selector:
    run: maliev-line-chatbot-service
  sessionAffinity: None
  type: NodePort
