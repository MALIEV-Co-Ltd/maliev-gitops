apiVersion: v1
kind: Service
metadata:
  name: line-chatbot-service
  namespace: maliev
  labels:
    run: line-chatbot-service
spec:
  ports:
  - port: 80
    targetPort: 8080
    name: http
  selector:
    run: line-chatbot-service
  sessionAffinity: None
  type: NodePort
