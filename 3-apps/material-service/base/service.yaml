apiVersion: v1
kind: Service
metadata:
  name: maliev-materialservice
  labels:
    app: maliev-materialservice
spec:
  type: ClusterIP
  selector:
    app: maliev-materialservice
  ports:
  - port: 80
    targetPort: 8080