apiVersion: v1
kind: Service
metadata:
  name: maliev-materialservice-api
  labels:
    app: maliev-materialservice-api
spec:
  type: ClusterIP
  selector:
    app: maliev-materialservice-api
  ports:
  - port: 80
    targetPort: 8080