apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-materialservice
  labels:
    app: maliev-materialservice
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-materialservice
  template:
    metadata:
      labels:
        app: maliev-materialservice
    spec:
      nodeSelector:
        cloud.google.com/gke-nodepool: backend-pool
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-materialservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-materialservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /materials/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /materials/readiness
            port: 8080
