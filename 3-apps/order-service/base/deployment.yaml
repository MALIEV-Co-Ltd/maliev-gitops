apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-orderservice
  labels:
    app: maliev-orderservice
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-orderservice
  template:
    metadata:
      labels:
        app: maliev-orderservice
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-orderservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev.orderservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /orders/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /orders/readiness
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        env:
        - name: ConnectionStrings__OrderDbContext
          valueFrom:
            secretKeyRef:
              name: order-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-string