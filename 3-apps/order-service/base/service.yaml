apiVersion: v1
kind: Service
metadata:
  name: maliev-orderservice-api
  labels:
    app: maliev-orderservice-api
spec:
  type: ClusterIP
  selector:
    app: maliev-orderservice-api
  ports:
  - port: 80
    targetPort: 8080