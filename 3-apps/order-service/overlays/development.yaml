apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-orderservice
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: maliev-orderservice
        livenessProbe:
          httpGet:
            path: /orders/liveness
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /orders/readiness
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Development"
        - name: ConnectionStrings__OrderDbContext
          valueFrom:
            secretKeyRef:
              name: order-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-string
