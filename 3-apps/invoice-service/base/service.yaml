apiVersion: v1
kind: Service
metadata:
  name: maliev-invoiceservice-api
  labels:
    app: maliev-invoiceservice-api
spec:
  type: ClusterIP
  selector:
    app: maliev-invoiceservice-api
  ports:
  - port: 80
    targetPort: 8080