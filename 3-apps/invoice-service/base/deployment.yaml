apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-invoiceservice-api
  labels:
    app: maliev-invoiceservice-api
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-invoiceservice-api
  template:
    metadata:
      labels:
        app: maliev-invoiceservice-api
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-invoiceservice-api
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev.invoiceservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /invoices/liveness
            port: 8080
          initialDelaySeconds: 45
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /invoices/readiness
            port: 8080
        env:
        - name: ConnectionStrings__InvoiceDbContext
          valueFrom:
            secretKeyRef:
              name: invoice-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-string