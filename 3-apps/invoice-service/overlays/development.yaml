apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-invoiceservice
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: maliev-invoiceservice
        livenessProbe:
          httpGet:
            path: /invoices/liveness
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /invoices/readiness
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Development"
        - name: ConnectionStrings__InvoiceDbContext
          valueFrom:
            secretKeyRef:
              name: invoice-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-string
