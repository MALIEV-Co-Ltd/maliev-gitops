apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: auth-service-secrets
spec:
  data:
  - secretKey: "ConnectionStrings:RefreshTokenDbContext"
    remoteRef:
      key: maliev-staging-auth-refreshtoken-db-conn