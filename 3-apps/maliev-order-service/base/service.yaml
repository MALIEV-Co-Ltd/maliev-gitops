apiVersion: v1
kind: Service
metadata:
  name: maliev-orderservice
  labels:
    app: maliev-orderservice
spec:
  type: ClusterIP
  selector:
    app: maliev-orderservice
  ports:
  - port: 80
    targetPort: 8080