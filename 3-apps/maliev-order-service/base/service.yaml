apiVersion: v1
kind: Service
metadata:
  name: maliev-order-service
  labels:
    app: maliev-order-service
spec:
  type: ClusterIP
  selector:
    app: maliev-order-service
  ports:
  - port: 80
    targetPort: 8080