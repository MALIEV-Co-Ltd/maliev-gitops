apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-order-service-secrets
spec:
  target:
    name: maliev-order-service-secrets
  dataFrom:
  - extract:
      key: maliev-prod-order-service-config
