apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-employeeservice
  labels:
    app: maliev-employeeservice
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-employeeservice
  template:
    metadata:
      labels:
        app: maliev-employeeservice
    spec:
      nodeSelector:
        cloud.google.com/gke-nodepool: backend-pool
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-employeeservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-employeeservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /employees/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /employees/readiness
            port: 8080
