apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-supplierservice
  labels:
    app: maliev-supplierservice
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-supplierservice
  template:
    metadata:
      labels:
        app: maliev-supplierservice
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-supplierservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev.supplierservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /suppliers/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /suppliers/readiness
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        env:
        - name: ConnectionStrings__SupplierDbContext
          valueFrom:
            secretKeyRef:
              name: supplier-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-string