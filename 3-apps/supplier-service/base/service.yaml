apiVersion: v1
kind: Service
metadata:
  name: maliev-supplierservice
  labels:
    app: maliev-supplierservice
spec:
  type: ClusterIP
  selector:
    app: maliev-supplierservice
  ports:
  - port: 80
    targetPort: 8080