apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-supplierservice
spec:
  template:
    spec:
      
      containers:
      - name: maliev-supplierservice
        livenessProbe:
          httpGet:
            path: /suppliers/liveness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /suppliers/readiness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Staging"
        - name: ConnectionStrings__SupplierDbContext
          valueFrom:
            secretKeyRef:
              name: supplier-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-string