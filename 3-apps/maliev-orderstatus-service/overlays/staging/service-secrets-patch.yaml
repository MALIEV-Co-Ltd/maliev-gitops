apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-orderstatus-service-secrets
spec:
  target:
    name: maliev-orderstatus-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-orderstatus-service-config
