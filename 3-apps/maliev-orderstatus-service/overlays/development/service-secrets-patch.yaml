apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: service-secrets
spec:
  target:
    name: maliev-orderstatus-service-secrets
  dataFrom:
  - extract:
      key: maliev-dev-orderstatus-service-config
