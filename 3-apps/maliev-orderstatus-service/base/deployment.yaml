apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-orderstatus-service
  labels:
    app: maliev-orderstatus-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-orderstatus-service
  template:
    metadata:
      labels:
        app: maliev-orderstatus-service
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-orderstatus-service
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-orderstatus-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /orderstatuses/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /orderstatuses/readiness
            port: 8080
