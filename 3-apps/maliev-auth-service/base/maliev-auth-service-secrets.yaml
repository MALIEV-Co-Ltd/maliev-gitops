# This defines the application-specific secrets for the maliev-auth-service.
# The `key` is a placeholder that MUST be overridden by each environment's overlay.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-auth-service-secrets # get overridden in overlays
spec:
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    # This will create a Kubernetes Secret named `maliev-auth-service-secrets`
    name: maliev-auth-service-secrets
  data:
  - secretKey: "ConnectionStrings:RefreshTokenDbContext"
    remoteRef:
      key: maliev-auth-refreshtoken-db-conn # get overridden in overlays
