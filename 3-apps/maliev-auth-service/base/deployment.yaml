apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-authservice
  labels:
    app: maliev-authservice
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-authservice
  template:
    metadata:
      labels:
        app: maliev-authservice
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-authservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-authservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /auth/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /auth/readiness
            port: 8080
        envFrom:
        - secretRef:
            name: maliev-shared-secrets
        - secretRef:
            name: maliev-auth-service-secrets
        