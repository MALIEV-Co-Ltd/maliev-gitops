apiVersion: v1
kind: Service
metadata:
  name: maliev-authservice
  labels:
    app: maliev-authservice
spec:
  type: ClusterIP
  selector:
    app: maliev-authservice
  ports:
  - port: 80
    targetPort: 8080