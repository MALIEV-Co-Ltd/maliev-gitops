apiVersion: v1
kind: Service
metadata:
  name: maliev-auth-service
  labels:
    app: maliev-auth-service
    monitor: "true"
spec:
  type: NodePort
  selector:
    app: maliev-auth-service
  ports:
  - port: 8080
    targetPort: 8080