apiVersion: v1
kind: Service
metadata:
  name: maliev-auth-service
  labels:
    app: maliev-auth-service
spec:
  type: NodePort
  selector:
    app: maliev-auth-service
  ports:
  - port: 80
    targetPort: 8080