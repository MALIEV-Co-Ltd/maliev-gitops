apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-auth-service-secrets
spec:
  target:
    name: maliev-auth-service-secrets
  dataFrom:
    - extract:
        key: maliev-prod-auth-service-config