apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: service-secrets
spec:
  target:
    name: maliev-auth-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-auth-service-config
