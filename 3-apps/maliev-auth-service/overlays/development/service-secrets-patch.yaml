apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: service-secrets
  namespace: maliev-dev
spec:
  target:
    name: maliev-auth-service-secrets
  dataFrom:
  - extract:
      key: maliev-dev-auth-service-config
