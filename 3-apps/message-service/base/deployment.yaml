apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-messageservice-api
  labels:
    app: maliev-messageservice-api
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-messageservice-api
  template:
    metadata:
      labels:
        app: maliev-messageservice-api
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-messageservice-api
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev.messageservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /messages/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /messages/readiness
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        env:
        - name: ConnectionStrings__MessageDbContext
          valueFrom:
            secretKeyRef:
              name: message-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-string