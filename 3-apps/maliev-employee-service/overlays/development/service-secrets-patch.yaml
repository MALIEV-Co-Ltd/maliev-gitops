apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-employee-service-secrets
spec:
  target:
    name: maliev-employee-service-secrets
  dataFrom:
    - extract:
        key: maliev-dev-employee-service-config
