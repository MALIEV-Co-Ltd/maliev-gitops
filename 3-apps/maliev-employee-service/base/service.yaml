apiVersion: v1
kind: Service
metadata:
  name: maliev-employee-service
  labels:
    app: maliev-employee-service
spec:
  type: NodePort
  selector:
    app: maliev-employee-service
  ports:
    - port: 8080
      targetPort: 8080
