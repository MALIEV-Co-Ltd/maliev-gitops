apiVersion: v1
kind: Service
metadata:
  name: maliev-contact-service
  labels:
    app: maliev-contact-service
    monitor: "true"
spec:
  type: NodePort
  selector:
    app: maliev-contact-service
  ports:
  - port: 8080
    targetPort: 8080