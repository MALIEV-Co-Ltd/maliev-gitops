apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-contact-service-secrets
spec:
  target:
    name: maliev-contact-service-secrets
  dataFrom:
  - extract:
      key: maliev-dev-contact-service-config
