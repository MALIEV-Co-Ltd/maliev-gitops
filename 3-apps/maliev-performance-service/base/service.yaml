apiVersion: v1
kind: Service
metadata:
  name: maliev-performance-service
spec:
  selector:
    app: maliev-performance-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
