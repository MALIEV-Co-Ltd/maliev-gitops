apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-receipt-service-deployment
spec:
  template:
    spec:
      containers:
      - name: receipt-service-container
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 500m
            memory: 1Gi
