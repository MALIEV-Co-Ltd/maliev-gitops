apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-receiptservice
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: maliev-receiptservice
        livenessProbe:
          httpGet:
            path: /receipts/liveness
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /receipts/readiness
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3