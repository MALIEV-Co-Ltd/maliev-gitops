apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-receiptservice
  labels:
    app: maliev-receiptservice
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-receiptservice
  template:
    metadata:
      labels:
        app: maliev-receiptservice
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-receiptservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev.receiptservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /receipts/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /receipts/readiness
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        env:
        - name: ConnectionStrings__ReceiptDbContext
          valueFrom:
            secretKeyRef:
              name: receipt-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-string