apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-web-deployment
spec:
  template:
    spec:
      containers:
      - name: web-container
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
