apiVersion: v1
kind: Service
metadata:
  name: maliev-message-service
  labels:
    app: maliev-message-service
spec:
  type: NodePort
  selector:
    app: maliev-message-service
  ports:
  - port: 80
    targetPort: 8080