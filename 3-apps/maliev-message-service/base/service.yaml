apiVersion: v1
kind: Service
metadata:
  name: maliev-messageservice
  labels:
    app: maliev-messageservice
spec:
  type: ClusterIP
  selector:
    app: maliev-messageservice
  ports:
  - port: 80
    targetPort: 8080