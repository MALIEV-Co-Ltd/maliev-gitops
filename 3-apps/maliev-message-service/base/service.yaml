apiVersion: v1
kind: Service
metadata:
  name: maliev-message-service
  labels:
    app: maliev-message-service
spec:
  type: NodePort
  selector:
    app: maliev-message-service
  ports:
  - port: 8080
    targetPort: 8080