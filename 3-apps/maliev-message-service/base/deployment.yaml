apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-message-service
  labels:
    app: maliev-message-service
spec:
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-message-service
  template:
    metadata:
      labels:
        app: maliev-message-service
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-message-service
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-message-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /messages/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /messages/readiness
            port: 8080
