apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: service-secrets
spec:
  target:
    name: maliev-message-service-secrets
  dataFrom:
  - extract:
      key: maliev-staging-message-service-config
