apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-message-service-secrets
spec:
  target:
    name: maliev-message-service-secrets
  dataFrom:
  - extract:
      key: maliev-dev-message-service-config
