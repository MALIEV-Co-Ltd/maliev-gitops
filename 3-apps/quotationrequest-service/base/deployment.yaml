apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-quotationrequestservice-api
  labels:
    app: maliev-quotationrequestservice-api
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-quotationrequestservice-api
  template:
    metadata:
      labels:
        app: maliev-quotationrequestservice-api
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-quotationrequestservice-api
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev.quotationrequestservice.api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /quotationrequests/liveness
            port: 8080
          initialDelaySeconds: 45
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /quotationrequests/readiness
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        env:
        - name: ConnectionStrings__QuotationRequestDbContext
          valueFrom:
            secretKeyRef:
              name: quotationrequest-db-conn
              key: connection-string
        - name: ConnectionStrings__LogDbContext
          valueFrom:
            secretKeyRef:
              name: log-db-conn
              key: connection-string