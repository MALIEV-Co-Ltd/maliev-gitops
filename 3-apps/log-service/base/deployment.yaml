apiVersion: apps/v1
kind: Deployment
metadata:
  name: maliev-logservice
  labels:
    app: maliev-logservice
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: maliev-logservice
  template:
    metadata:
      labels:
        app: maliev-logservice
    spec:
      
      terminationGracePeriodSeconds: 30
      containers:
      - name: maliev-logservice
        image: asia-southeast1-docker.pkg.dev/maliev-website/maliev-website-artifact/maliev-logservice
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /logs/liveness
            port: 8080
        readinessProbe:
          httpGet:
            path: /logs/readiness
            port: 8080
