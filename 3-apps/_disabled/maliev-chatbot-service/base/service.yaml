apiVersion: v1
kind: Service
metadata:
  name: maliev-chatbot-service
  labels:
    app: maliev-chatbot-service
spec:
  type: NodePort
  selector:
    app: maliev-chatbot-service
  ports:
    - port: 8080
      targetPort: 8080
