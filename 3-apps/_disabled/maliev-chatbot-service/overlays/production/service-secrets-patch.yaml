apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-chatbot-service-secrets
spec:
  target:
    name: maliev-chatbot-service-secrets
  dataFrom:
    - extract:
        key: maliev-prod-chatbot-service-config
