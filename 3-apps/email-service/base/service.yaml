apiVersion: v1
kind: Service
metadata:
  name: maliev-emailservice
  labels:
    app: maliev-emailservice
spec:
  type: ClusterIP
  selector:
    app: maliev-emailservice
  ports:
  - port: 80
    targetPort: 8080