apiVersion: v1
kind: Service
metadata:
  name: maliev-compensation-service
spec:
  selector:
    app: maliev-compensation-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
