apiVersion: v1
kind: Service
metadata:
  name: maliev-pricing-service
spec:
  selector:
    app: maliev-pricing-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
