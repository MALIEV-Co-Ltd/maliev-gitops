apiVersion: v1
kind: Service
metadata:
  name: maliev-pricing-service
  labels:
    app: maliev-pricing-service
spec:
  type: ClusterIP
  selector:
    app: maliev-pricing-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
