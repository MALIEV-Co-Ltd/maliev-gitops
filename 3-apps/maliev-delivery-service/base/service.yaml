apiVersion: v1
kind: Service
metadata:
  name: maliev-delivery-service
  labels:
    app: maliev-delivery-service
spec:
  type: NodePort
  selector:
    app: maliev-delivery-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
