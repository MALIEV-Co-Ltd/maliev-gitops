apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: maliev-delivery-service-secrets
spec:
  target:
    name: maliev-delivery-service-secrets
  dataFrom:
    - extract:
        key: maliev-dev-delivery-service-config
